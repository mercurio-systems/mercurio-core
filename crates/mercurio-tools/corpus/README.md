# Pilot Corpus Seeds

`pilot_corpus.seed.json` is maintainer tooling data for audit and comparison
binaries that run Mercurio against selected Pilot examples.

It is not part of stdlib extraction, runtime compilation, KPAR packaging, or the
stdlib support MPack.
